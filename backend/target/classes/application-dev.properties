# Dev profile - H2 in-memory (no PostgreSQL required for local run)
# Use: mvn spring-boot:run -Dspring.profiles.active=dev

spring.datasource.url=jdbc:h2:mem:resume_analyzer;DB_CLOSE_DELAY=-1
spring.datasource.username=Vipin
spring.datasource.password=Vipin@2507
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# H2 console (optional, for debugging)
# spring.h2.console.enabled=true
